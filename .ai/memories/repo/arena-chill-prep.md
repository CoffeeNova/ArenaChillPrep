# ArenaChillPrep — repo memory

Working record for AI agents. Source of truth is `.ai/`; this mirrors verified outcomes and phase status.

## Status (2026-08-11)

- **Workflow Clone button (2026-08-24, user-requested):** the Workflows editor can now clone a workflow into a new slot — row 1 of the Workflow editor gained a `Clone` button between `+ Add` and `Delete` (`WorkflowUI:cloneWorkflow` → `WorkflowRepository:cloneWorkflow(sourceSlot)`). The clone appends a NEW slot at the end: name + `enabled` + steps are DEEP-copied (`Utils/Tables:deepCopy` — mutating the copy never touches the source), the key binding is NOT copied (a clone must be bound separately), a non-empty name gets a localized `copyNameSuffix` (` (copy)` / ` (копия)`) so the selector stays unambiguous, and the new slot is selected. At the slot limit it prints the same `slotLimit` message as `+ Add`; a missing source definition returns nil without touching `slotCount`. **Layout:** the three buttons use measured widths (`workflowButtonLayout()` — `textWidth(label) + BUTTON_PAD*2`, cached, same pattern as `actionLayout`) so all three fit one row in BOTH locales; the button labels were shortened (`+ Add workflow` → `+ Add`, `Delete Workflow` → `Delete`, ruRU `+ Добавить` / `Удалить`) because the full ruRU labels could never fit three-across in the 632 px content width. **Overlap fix (2026-08-24, user-reported):** the first attempt started the row at `FIELD_X + 180` — the `Clone` button overlapped the workflow selector dropdown (its RENDERED width exceeds the 170 px passed to `UIDropDownMenu_SetWidth`); the row now starts at `FIELD_X + 240`, the original `+ Add workflow` position. Contract-first: CONTEXT.md + ARCHITECTURE.md §2.8 (bullet 3 + repository method list) updated. Checks: 278/278 tests @ 92.11% (4 new: deep-copy isolation, empty-name no suffix, slot limit, missing source), syntax 65 files OK, vararg 33 OK, deployed 36 files. In-game DoD pending: clone a populated workflow, verify name suffix + steps + selection + no key conflict.

- **Step replace-in-place (2026-08-24, user-requested):** a workflow step can now be CHANGED to another spell/item directly in the Workflows tab — the spell cell of every step row is a dropdown with the same catalog as "+ Add step"; selecting an entry REPLACES the step at the SAME index (the row keeps its position; the new step is rebuilt by the `buildStep` factory, so type/target/skip come from the defaults of the NEW spell). Implementation: `WorkflowRepository:replaceStep(slot, index, spellKey)` + the addStep resolution extracted into `resolveNewStep(spellKey)` (shared by addStep/replaceStep); `WorkflowUI:replaceStep` + `buildStepRow` spell cell now an `UI.Dropdown` (`ACPWorkflowStepSpell<slot>_<idx>`, getter nil → collapsed label shows the current spell via the placeholder `text`, 22 px tall); new `L.workflow.changeStepTooltip` (enUS+ruRU). **Width fix (2026-08-24, user-reported overflow into the Target column):** the dropdown is now sized by `spellCellWidth()` — the longest possible collapsed label (spellItems labels + per-rank stone labels + `petStepLabel`-formatted pet labels + equip-item names, incl. localized `GetItemInfo` names) measured in the dropdown's own font (`GameFontHighlightSmallLeft`) + `SPELL_CELL_CHROME` (26 px), clamped to the column width; the cache resets on `ACP_SPELLBOOK_CHANGED`. **Metadata line removed (2026-08-24, user-reported):** the per-step `SpellID: <id>`/`ItemID: <id>` line and the `pet ability` hint were REMOVED as redundant → rows are now single-line, `STEP_ROW_H` 48→40; dead localization keys `spellIDLabel`/`itemIDLabel`/`petStepHint` removed (enUS+ruRU). Contract-first: CONTEXT.md + ARCHITECTURE.md §2.8 updated. Checks: 274/274 tests @ 91.99%, vararg 33 OK, syntax 65 OK. In-game DoD pending: replace a step mid-list (position kept), replace cast↔equipItem, dropdown no longer spills into the Target column, single-line rows look clean.

- **W15 cleanup DONE (2026-08-24, per user /dump results):** (1) `GetPlayerAuraBySpellID` exists on 20506 → the ArenaPrep index-based `GetAuraDataByIndex` fallback was dead-on-target → DELETED (`scanBuff` now returns a single boolean from the direct lookup; `MAX_AURA_INDEX`/`GetAuraDataByIndex` alias removed; the `testScanBuffFallback` test removed). (2) The prep aura reports expirationTime=0 → the `buffExpirationTime` field, its checkNow assignments and the getRemainingTime fallback branch were dead on 2.5.5 → DELETED (`testGetRemainingTimeFromAura` removed; scanBuff no longer returns expiration). (3) `C_Item.GetItemGUID` + `ItemLocation.CreateFromBagAndSlot` exist on 20506 → the TradeManager getItemGUID path is LIVE (called per placement) → the always-true API-existence guards REMOVED (kept: bag/slot nil check for ITEM_UNLOCKED + the DoesItemExist shim guard; `testGetItemGUIDNoApi` removed). **The architecture refactoring is COMPLETE** — all 8 phases done, user-verified in game (full 2v2 cycle + both slot keys + all 5 step types). Verification: 274/274 tests, coverage 92.02%, syntax 65 files OK, vararg 33 files OK, deployed 36 files.

- **Phase 11 — integration review + docs DONE (2026-08-24):** code-level integration and edge-case review of the workflow engine vs the autotrade. Verified: the two pipelines are independent and event-driven (workflow createItem → BAG_UPDATE → ACP_ITEMS_CHANGED → DeliveryController trades; read-only Inventory is the only shared surface — no double-trade, no shared mutable state; a trade window opening mid-workflow cannot corrupt the engine because a blocked press simply waits — the engine is user-paced). All 16 §5 edge cases are handled in code (one deliberate design evolution vs the plan table: cast INTERRUPTED re-arms the SAME step in waitingForKey instead of pausing — documented 2026-08-21; pause-on-move is unconditional). Docs (contract-first): ARCHITECTURE.md gained the missing **§2.10 WorkflowEngine** (state machine diagram, event-driven step execution, STEP_DISPATCH, gates, skip-if-buffed, extracted sub-systems, integration note) + 17 workflow rows in the §4 edge-case table. **REMAINING (user in-game):** the Phase 11 in-arena DoD — **DONE per user 2026-08-24** ("проверил полный цикл — все работает").

- **Refactor follow-up per user verification (2026-08-24):** the user verified Phases 0-7 IN GAME ("все работает как надо") and made two decisions: (1) **`Classes/SpellbookScanner.lua` DELETED** — the live scan never ran and the code was clutter; `WorkflowSpellbook:scan()` now rebuilds the catalog from the static data only (`reset → mergeStaticWarlock → addStaticFallback → finalize`; warlock = full static catalog, other classes = empty; PLAYER_LOGIN/SPELLS_CHANGED rebuilds unchanged; the `scanBook` delegate removed; the dead scan test removed, the fallback test renamed `testRebuildFillsStaticCatalogForWarlock`; the luacov exclusion note removed with the module). (2) **Second slot key FIXED** (user: second key assignable in Key Bindings but the workflow never started/cast): `WorkflowBindings:applySlotBindings` now captures BOTH `GetBindingKey(action)` returns and puts the priority override on BOTH keys (the second key previously only fired the ACP_WORKFLOW<i> start action — a no-op while running — and could never cast); `WorkflowKeybindController:setSlotKey` now reads `local old1, old2 = GetBindingKey(command)` (the old `GetBindingKey and GetBindingKey(command)` dropped the second value — `and` truncates) and clears BOTH on rebind, with `GetBindingKey` added to the API guard. Regression tests: `testApplySlotBindingsOverridesSecondaryKey` (both keys override to the slot button) + `testSetSlotKeyClearsBothKeys`. Verification: 277/277 tests, coverage 91.93%, syntax 65 files OK, vararg 33 files OK, deployed 36 files. GOTCHA (verified under LuaJIT): `return f() and g()` yields ONE value even in return position — `and`/`or` truncate multiple return values; capture explicitly (`local a, b = g()`).

- **Architecture refactor Phases 5-7 DONE (2026-08-24, per `.ai/docs/architecture-review-refactoring-plan.md`):** the god-object splits. **Phase 5:** WorkflowEngine split into core + `WorkflowCastController` (castSpell/requestKeyCast/onKeyPressed/waits/UNIT_SPELLCAST_* events), `PetAbilityCaster` (petMacroText/isPetAbilityApplied/armPetVerify/petAbility/onSecurePress), `WorkflowItemSteps` (createItem/equipItem/expected-item helpers/ACP_ITEMS_CHANGED) + `WorkflowBindings` absorbed the secure-button creation + binding management (createButtons/applyBinding/applySlotBindings/resolveCastKey/setCastAttribute/onPreClick/binding events). The engine keeps the state, gates, catalog/aura helpers, lifecycle and **`STEP_DISPATCH` — the W9 per-step-type handler table** `{run, goalMet?, skippable?, needsKnownSpell?, petDoneAware?, petExempt?}` (the old if/elseif chain is in git history HEAD 9f0cb9e). Extracted modules operate ON the engine (first arg); the engine exposes thin delegates so the public surface (UI/OptionsUI/tests) is unchanged. `WORKFLOW_REASON` moved to Data.Constants. **Phase 6:** WorkflowUI is pure layout/render; `WorkflowRepository` owns CRUD + `buildStep` step factory (type/target/skip inference) + findSpell; new `WorkflowKeybindController` owns SetBinding/SaveBindings/conflict-steal + shiftBindingsAfterDelete; `bindPath(path)` helper collapses the getter/setter closures; `renderSteps` preserves scroll position (W17 fixed — capture before rebuild, restore + clamp); Widgets Keybind fallback strings are now caller-passed L.* values. **Phase 7:** WorkflowSpellbook is a facade over `SpellbookScanner` (probing + scan loop), `SpellbookCatalogBuilder` (assembly + rank→item maps + reads), `WarlockCatalogExtender` (class-gated fallback/merge) and `SpellbookLabels` (stoneStepLabel; DEAD `stoneName` removed — no callers since the rank-dropdown removal). Settings keeps the dot-path store; `SettingsMigrator` owns migrateWorkflowNames/migrateStepSpellIDs/migratePlaceholderDefinitions/normalizeRankKeys/ensureDefaults. **SKIPPED:** Phase 7 item 3 (unify Data/Workflows spells.createItem + stoneRanks) — soulstone rank data exists ONLY in createItem, unification would restructure data and risk behavior; deferred. **TWO LATENT BUGS FOUND + REPORTED, NOT FIXED (behavior-preserving):** (a) the spellbook probe pattern `return X and pcall(...)` truncates pcall's 2nd value → count always nil → the REAL scan never runs, catalog = static fallback (matches the user's rejected full-scan exposure; SpellbookScanner EXCLUDED from coverage include with documentation — see ADR 17); (b) `WorkflowKeybindController:setSlotKey` reads `GetBindingKey and GetBindingKey(command)` → truncation → a slot's SECONDARY key is never cleared (verbatim from old WorkflowUI; one-line fix pending decision). GOTCHA: `and` truncates multiple return values even in return position (`return f() and g()` yields ONE value — verified under LuaJIT); the repo's pcall-capture pattern (`local ok, res = pcall(...)`) is the fix form. Verification: **277/277 tests** (new suites test_workflowrepository + test_workflowkeybindcontroller; + targeted tests for builder/labels/extender/migrator), coverage **91.80%**, syntax 66 files OK, vararg 34 files OK. In-game deploy + verification of ALL 5 step types + the Workflows tab PENDING (the plan's Phase 5/6 risk mitigation: live-verify cast/summon/createItem/equipItem/pet + screenshot the editor).

- **Architecture refactor Phases 0-4 DONE (2026-08-24, per `.ai/docs/architecture-review-refactoring-plan.md`):** behavior-preserving consolidation of the two god objects without splitting them. **Phase 0:** `Classes/WorkflowEngine$` added to the luacov include list (baseline: engine 67.79%, total 87.30% — the gate was failing). **Phase 1** (dead code/dedup): dead `ACP_TRADE_START` fire removed (+ test updated); duplicate `expectedItemID=nil` in `pause()` removed; dead duplicate `spellTypeIsSpell` removed; 6 dead localization keys removed (keybindHeader/keybindHint/slotBinding/notBound/keyStatusLabel/activationKeyLabel, enUS+ruRU; `bindingWorkflow`/`bindingsInit` are live — keep); `OptionsUI:setSetting` no longer double-persists; `WorkflowUI:persistSettings` nil-guard dropped; always-true API guards removed (spellLabel/itemLabel/selectedKey/SetVerticalScroll); `attachTooltip`/`isSafeBindingKey` now exported from Widgets (`ACP.UI.*`, WorkflowUI reuses; the shared `attachTooltip` gained `EnableMouse(true)` for FontStrings); `Tests/helpers.deepCopy` now reuses `Utils/Tables:deepCopy`; commented food/water stubs removed from Data/Items; `HEALTHSTONE_RESULTS`/`SOULSTONE_RESULTS` derived from the Data/Items catalog (soulstones table added to Items; min-ID-per-rank = the old primary variant); `CLASS_WARLOCK` guard consolidated into `Data.Constants.CLASS_WARLOCK` (Items/Spellbook/WorkflowUI consume it); `GATE_SAFETY_DEFAULT = 15` in Constants replaces the `or 15` fallbacks. PLAN STALENESS: `OptionsUI.rankToName` is NOT dead (read at :227) — kept. **Phase 2** (enums/helpers): `WORKFLOW_STATE`/`DELIVERY_STATE` enum tables in Constants replace ALL raw state-string comparisons in both orchestrators; WorkflowEngine module-local `Reason` table for the 14 pause reasons; `WorkflowEngine:clearTransientState(includePetDone)` extracted (advance/pause/reset/start/onCastTimeout — 5 duplicated blocks gone); `hasBuff(unit,name)` replaces both aura loops; `isWaitingForKeyOrCast`/`isRunningCastStep` predicates collapse the 6 event guards; new `Classes/WorkflowRepository.lua` (`definitionPath/stepsPath/stepPath`) replaces 12+ re-typed dot paths in WorkflowUI + WorkflowEngine; reverse layering removed — `Settings:reset` fires `ACP_SETTINGS_RESET` (OptionsUI subscribes) and `WorkflowSpellbook:_refreshUI` fires `ACP_SPELLBOOK_CHANGED` (WorkflowUI subscribes on build). **Phase 3** (TradeManager layering): `queueConfiguredItems` moved to new `Classes/TradePlanner.lua` (`buildQueue`/`categoryReady`/`getCategories`/`selectedRanksByGroup`); TradeManager restored to true low-level (`startTrade/queueItems/cancel/getPartner`); on `ACP_TRADE_OPENED` the DeliveryController fills the queue via `TradePlanner:buildQueue()`; rank grouping is single-source (DC delegates); `TradeFrame` nil-guards + `useContainerItem` guard fixed (W14). **Phase 4** (shared Preconditions + StateMachine mixin): new `Classes/Preconditions.lua` (`enabled/inArena/buffActive/notInCombat/notInLockdown/notDead/gateSafetyOk`) and `Classes/StateMachine.lua` (`initOnce(embedder)` + `setState(embedder, newState, allowed)` with enum validation); both orchestrators embed the mixin via one-line wrappers; `canStartTrade`/`checkGates` delegate the shared gates; `castAccepted` clearing centralized into `clearTransientState` (W11 fixed). **COMBAT-API DECISION (plan open question 4):** both APIs kept — `UnitAffectingCombat` for trading, `InCombatLockdown` for casting (they test different things). **OPEN QUESTION 2 (castAccepted)** resolved by applying the plan's own Phase 4 fix. Verification: 238/238 tests, coverage **93.26%** (engine 83.10%, TradePlanner 100%, StateMachine 100%, Preconditions 95.65%), syntax 55 files OK, vararg 25 files OK. GOTCHA: a mixin written as `function M:setState(newState, allowed)` receives the EMBEDDER as `newState` when called `M:setState(self, ...)` — the embedder must be an explicit param (`function M:setState(embedder, newState, allowed)`); and new modules must be added to BOTH the TOC and `Tests/loader.lua` (the loader has its own file list, not TOC-parsed). In-game deploy + verification PENDING; Phases 5-7 (god-object splits) deferred to a user decision.

- **v0.1.0 released** (2026-08-09) — Warlock only: auto-trades Healthstones of all ranks to the arena partner during arena preparation (TBC Anniversary, Interface 20506). Default bracket **2v2 only** (3v3/5v5 code kept, checkboxes locked in the UI for v0.1).
- **Automated unit-test suite added** (2026-08-11): `Tests/` runs outside the game under LuaJIT (luaunit 3.5 + luacov 0.17). **169 tests, 96.45% line coverage** of all non-UI modules (bootstrap, Data, Utils, Classes logic); `OptionsUI.lua` excluded (UI code). Runner: `Tests/run-tests.ps1` (exit `0` = pass + coverage ≥ 90%).
- **AI library migrated to `.ai/`** (2026-08-18): the agent toolkit moved from `.github/` to `.ai/` (same layout as the sibling addon BloomBuddy) — entry point `AGENTS.md` → `.ai/CONTEXT.md` → `.ai/ARCHITECTURE.md` → skills. `.github/` now holds only GitHub Actions workflows (none yet).
- **Settings UI redesigned** (2026-08-18, per `.ai/docs/ui-redesign-plan.md`): `OptionsUI.lua` split into presentation (`Classes/UI/Widgets.lua` — `ACP.UI.*` widgets + geometry constants) and logic (subcategory registration, layout, sync). Panel has tabbed subcategories **General** (master switch, "Reset to defaults" button via `Settings:reset()`) and **Autotrade** (left column: brackets + timing with a divider between them; right column: ranks). Tooltips on every control; master switch off grays out Autotrade (3v3/5v5 stay permanently locked). **170 tests, 96.54% coverage.**
- **Workflow-engine feasibility researched** (2026-08-18): the one-button warlock prep pipeline (buff/summon/create stones/pass + keybinds + pause-on-move) is FEASIBLE on 2.5.6 out of combat. Full evidence index in `.ai/docs/workflow-engine-research.md`; verified facts folded into the `wow-api-20506` and `addon-research` skills. Hard limit: autonomous multi-step casting during combat is impossible (protected APIs; secure buttons = one action per hardware event).
- **Phase 7 — workflow engine data layer DONE + in-game verified** (2026-08-18): `Data/Workflows.lua` (warlock spell catalog in `buffs/summons/createItem/utility`, `validateStep` per §3.5 schema, `isValidTarget`), `Constants` (`WORKFLOW_MAX_SLOTS`/`GCD_TICK`/`CAST_TIMEOUT`/`TARGETS`/`STEP_*`, `SOUL_SHARD_ITEM_ID`=6265), `DefaultSettings.workflows` (enabled, pauseOnMove, skipIfBuffedDefault, 5 `definitions` slots, slot 1 = default "Full prep": Fel Armor 28176 self → Summon Succubus 712 → Create Healthstone 6201/22105), `Localization.workflow` (enUS+ruRU), TOC entry after `Data/Localization.lua`. Corrected plan placeholders verified in game: Fel Armor `28176` (plan's `28276` = Lightwell Renew on this client), Create Healthstone `6201` (TBC ranks 6201…27230; plan's `116761` was a non-TBC ID), Ritual of Souls `29893` (`58887` is WotLK rank 2). In-game checks: `/dump ACP.Data.Workflows.spells` (full catalog), `validateStep({type="cast", spellID=28176, target="player", skipIfBuffed=true})` → `true`, `Settings:get("workflows.definitions.1.steps.1.type")` → `"cast"`. Note: the first in-game attempt showed `ACP.Data.Workflows == nil` because the client folder still had the old v0.1 copy — **`tools/deploy.ps1` must be run after code changes** before in-game verification. Still unverified (marked `-- TODO` in `Data/Workflows.lua`): Fel Armor TBC max-rank ID (28275/28276 are other spells; rank 1 used, casting is name-based so cosmetic), Create Soulstone 693 rank-1 ↔ itemID 22103 rank-pairing (name-based cast yields 22103 at max level).

- **Phase 8 — engine pivot: ALL steps via one bound hotkey** (2026-08-19): In-game testing proved **all insecure casting is BLOCKED on 20506 outside safe zones — cast-time AND instant**. Reproduced in a safe zone, OOC (`inCombat=false affectingCombat=false`, 22 shards): cast-time spells (Summon Succubus 712, Inferno, Create Healthstone, Shadow Bolt) → `ForceTaint_Strong` popup. In the OPEN WORLD: even instant `CastSpellByName("Fel Armor")` pops "blocked from an action" (no GCD, no buff; engine reported "workflow cast dropped (instant)"). `SecureActionButtonTemplate:Click()` and `RunBinding()` (spell/macro) are also blocked — **only a real hardware key press can cast on 20506.** User approved **all steps via F9**: every step (buff, summon, createItem) points a hidden secure button (`ACPWorkflowButton`, `SetAttribute("spell", name)`, `RegisterForClicks("AnyDown")`, hidden) at the spell and waits for ONE hotkey (`/acp bind <key>`, default nil → `noHotkey` pause; `SetBindingClick(key, "ACPWorkflowButton")` + `SaveBindings`, retry while no binding set/in combat, unbind old key on change). `UNIT_SPELLCAST_SENT`/`START` → `onKeyPressed()`: cast-time steps → `waitingForCast` + 10 s timeout, completion via STOP/SUCCEEDED; instant steps → `waitForGCD` (poll natural GCD; STOP/SUCCEEDED may not fire for instant), with the buff-must-land check as a safety net. After DONE/pause/reset the button resets to an inert spell so stray F9 does nothing (fixed 2026-08-19). M6-style `/castsequence` macro rejected (255-char limit, no state). Docs updated first (contract-first): `wow-api-20506` skill gotcha row, plan §3.6 + status, research exec summary. **Verified in game (2026-08-19, open world, `/acp workflowtest 1`):** F9 summoned the Succubus and created a Master Healthstone → DONE; `/acp status` → `workflow: DONE | slot=1 | step=4/3 | key=F9` (step=4/3 is the documented post-DONE increment); blocked instant cast → PAUSED with a clear diagnostic. **FULLY VERIFIED IN GAME (2026-08-19, open world, `/acp workflowtest 1`):** F9 → Fel Armor buff lands → F9 → Summon Succubus cast bar → F9 → "You create: [Master Healthstone]" → DONE, all three steps one-by-one; stopping a cast mid-way (move/ESC) then pressing F9 re-casts the interrupted step ("now it is perfect" — user, 2026-08-19). **Remaining DoD (optional, user prefers open-world):** full in-arena prep run (`/acp workflow 1` with the real buff; `ACP_BUFF_LOST` → IDLE) — autotrade phases 1–5 already live-verified in arenas.

- **Phase 9 — Keybindings + unified key IMPLEMENTED (2026-08-19):** `Classes/WorkflowBindings.lua` (BINDING_HEADER_ACP + BINDING_NAME_ACP_WORKFLOW1..5 via `L.workflow.bindingWorkflow`, global handlers `ACP_WORKFLOW1..5()` → `start(N)`), `Bindings.xml` referenced from the TOC. **On 20506 the legacy XML parser logs `Unrecognized XML: Binding` for every `<Binding>` (noise only, non-fatal)** — the KeyCommand system still registers the actions from the `BINDING_*` globals once the file is TOC-referenced. `category="ADDONS"` buried the entries; `category="BINDING_HEADER_ACP"` (resolves via `_G[category]` to "ArenaChillPrep") shows them under their own category — verified by the user (bound F5 → Workflow 1). **Unified key cast flow:** the workflow key IS the cast key — while waiting for the press `takeoverCastKey` re-points it at `ACPWorkflowButton` via `SetBindingClick` (saved in `self._takeover`), `releaseCastKey` restores it on press/pause/reset/DONE/`ADDON_UNLOADING`. `resolveCastKey` prefers the workflow key over the `/acp bind` hotkey. The takeover is NOT `SaveBindings`'d (persisting it clobbered the player's binding on `/reload` — verified F5 loss 2026-08-19). `getDefinition` ignores per-slot `enabled` under the debug bypass so `/acp workflowtest <N>` tests ANY slot; empty workflows print `emptyWorkflow`. **FATAL LOAD CRASH fixed (2026-08-19):** `GetCurrentBindingSet()` returns `0` (not nil) before bindings load at `ADDON_LOADED`; `SaveBindings(0)` threw `Usage: SaveBindings(1||2)` inside `applyBinding` → aborted `_init` → addon dead, `/acp` unregistered (logged in `Logs/General.log`; 0 is truthy in Lua so the old truthiness guard missed it). All `SaveBindings` call sites now use a `bindingSet()` helper (explicit `1|2` or nil; retry while not loaded). **`ADDON_UNLOADING` is NOT registerable on 20506** — `RegisterEvent("ADDON_UNLOADING")` throws `Attempt to register unknown event "ADDON_UNLOADING"` (live error 2026-08-19 22:58; crashed `_init`); the logout restore uses `PLAYER_LOGOUT` instead and `/reload`-safety relies on the takeover being in-memory only (reload re-reads saved bindings). Gotchas recorded in the `wow-api-20506` skill. Checks green (170 tests @ 97.04%, vararg 19, syntax 47). **In-game DoD pending user verification.**

**Bindings.xml auto-load (2026-08-19):** on 20506 the client AUTO-LOADS a file named `Bindings.xml` from the addon folder by filename convention — it MUST NOT be listed in the TOC. Listing it made the client double-parse it with the UI XML parser → `Unrecognized XML: Binding` / `Unrecognized XML attribute: name` error popups per entry (15 on /reload with scriptErrors on). Root-caused by checking every addon in the client folder: NOT ONE working addon (BetterFishing, GatherMate2, AtlasLoot, Details, FishingBuddy) references Bindings.xml in its TOC, and none log the warning. Fix: removed the TOC line; `deploy.ps1` now ships `Bindings.xml` explicitly (it only copies TOC-referenced files, so the auto-load convention would otherwise skip it). `category="BINDING_HEADER_ACP"` still correct for a named category. **Phase 9 COMPLETE + in-game verified (2026-08-19):** with the TOC fix deployed, `/reload` produces NO Bindings.xml error popups and the workflow key still starts/casts each step; binding persists across /reload. Phase 8 was already verified in-game earlier (open-world workflowtest run: Succubus + Master Healthstone, DONE). Remaining Phase 8 DoD: one real in-arena prep run (`/acp workflow 1` with the actual buff; pause/resume on move; `ACP_BUFF_LOST` → IDLE) — still optional.

- **m6-macro pre-defined workflows + equipItem step type (2026-08-20):** Slots 1-2 in `DefaultSettings.workflows.definitions` now ship the user's actual m6 arena-prep macros as steps. Verified spell IDs: `27230` = Create Healthstone Rank 6 → Master Healthstone `22105`; `11730` = rank 5 → Major HS `19012` (GladiatorlosSA2 spelllist_TBC); `28172` = Create Spellstone Rank 4 → **Master Spellstone `22646`** (NOT 5523 — that's Small Barnacled Clam; Questie tbcItemDB); `28189` = Fel Armor Rank 2; `132` = Detect Invisibility; `19028` = Soul Link. Macro entries "Create Healthstone(Rank 5)" are stored as `27230/22105` because the client auto-upgrades old-rank name casts to the max rank at 70 (a 11730/19012 step would stall — the item never matches). **New step type `equipItem`** (schema: `{type="equipItem", itemID, itemName?}`): the `/equip Master Spellstone` macro line. Engine re-points the SAME secure button to `type="item"` + `item=<localized name>` (GetItemInfo — the stone is in bags so the info is cached) and waits for the key press; completion is POLL-DRIVEN (0.25 s until `countItem(itemID) == 0` = equipped; no UNIT_SPELLCAST_* fires for item use) and user-paced (no timeout). `requestKeyCast`/`clearKeyCast` always restore `type="spell"` (a stale "item" type would make stray presses USE items). **Goal-met fast paths:** `createItem` advances when the item is ALREADY in bags (no press — the macros' duplicate HS entries need one press total and can never stall on "already have one" cast rejections); `equipItem` advances when the item is NOT in bags (already equipped). `executeCurrentStep` dispatches equipItem BEFORE the spell-only knowsSpell/skipIfBuffed checks. UI: step rows render item name + `ItemID: <id>` (no rank/target/skip — all "Not available"), Add Step gained an **Equip items** category (`Data/Workflows.equipItems` = spellstone family 5522/13602/13603/22646; "item:<id>" value convention). Catalog additions: Fel Armor 28189, Detect Invisibility 132, Create HS 11730/27230, Create Spellstone 28172 (+ `getEquipItem`). **Settings merge fix discovered via tests:** `deepMerge` index-merges arrays → a saved placeholder definition's 3 steps got PREPENDED to the new default 14 steps (hybrid). The workflows branch now merges **per-slot REPLACE** (saved `definitions[N]` wins wholesale) and `ensureDefaults` is **array-aware** (missing step indices are never filled from defaults); `migratePlaceholderDefinitions` swaps definitions that still exactly match the OLD placeholders (name + steps deep-equal) for the new defaults BEFORE the merge — user edits are never touched (fresh profiles already have the new defaults post-merge, so they're not re-swapped). **Checks:** 192/192 tests @ 95.95% (new `Tests/Data/test_workflows.lua` suite + defaults/constants/settings migration tests), vararg 21 files, syntax OK, LuaHelper clean. `equipItem` secure-button flow still needs in-game verification (the type="item" path is the M6-equivalent of /equip but untested live).

## Verified client facts (TBC Anniversary 2.5.x — verified in game phases 1–5 + working addons)

- **Prep buff:** `C_UnitAuras.GetPlayerAuraBySpellID(32727)` returns an aura object. `UnitBuff("player", name)` crashes on 2.5.5 (deprecated wrapper proxies to `C_UnitAuras.GetBuffDataByIndex`, numeric index only); `UnitAura(unit, i, filter)` returns shifted legacy positions with no spellID/expirationTime.
- **Gate countdown is NOT the aura:** the prep buff reports `duration=0`/`expirationTime=0`. The working source (ArenaAnalytics + sArena on the same client) is `CHAT_MSG_BG_SYSTEM_NEUTRAL` + the localized message map (`ACP.Data.Constants.ARENA_COUNTDOWN_MESSAGES`, 60/30/15/0 s, enUS + ruRU). `GetBattlefieldTimeRemaining()` is a battleground match timer, not the pre-gate countdown.
- **Bracket detection:** `GetNumPartyMembers() + 1` → 2/3/5 (the group is locked inside the arena, so it is stable during prep); cross-check `GetNumArenaOpponents()` (2.5.1+, may be 0 briefly at load). `GetNumPartyMembers` is the TBC name — `GetNumSubgroupMembers` only exists from 5.0.4+.
- **Trading (Gargul patterns, same client):** `InitiateTrade(unit)` → `TRADE_SHOW`; place items with `UseContainerItem(bag, slot)` (the game auto-places into the next free slot); one item per tick (FIFO + ~0.15 s ticker) — too-fast adds get silently removed; `ITEM_UNLOCKED` re-add for items removed within 0.5 s; completion = `UI_INFO_MESSAGE` (SECOND arg) == `ERR_TRADE_COMPLETE`, never `TRADE_CLOSED` alone; `ClearCursor()` on close.
- **Auto-accept is IMPOSSIBLE on 2.5.x** — `AcceptTrade()` is restricted (requires a hardware event); a programmatic call or `TradeFrameTradeButton:Click()` is silently blocked (`Interface action failed because of an AddOn`). The addon places items; the player confirms manually.
- **`C_Timer` handle `Cancel()` is UNRELIABLE (verified 2026-08-10):** a "cancelled" timer can still fire — a cancelled `TradeOpen` timer fired after `TRADE_SHOW` (false timeout killed a live trade, lost the partner attribution → repeat trades). Fix pattern in `Utils/Timers.lua`: named entries `{active = true, handle}`; the callback bails unless `active` and still registered; `handle:Cancel()` is best-effort.
- **Container API:** `GetContainerNumSlots` is NOT a global (only `C_Container`); `C_Container.GetContainerItemInfo` returns an OBJECT (not the legacy 11-tuple); `C_Item.GetItemGUID(bag, slot)` crashes — needs an `ItemLocation`. Call-time shims in `Utils/Items`.
- **`CLASS_*` constants are NOT defined** on TBC FrameXML — guard `local CLASS_WARLOCK = _G.CLASS_WARLOCK or "WARLOCK"`.
- **Settings keys:** `[19013]` (number) and `"19013"` (string) are DIFFERENT Lua keys — dot-path get/set with string segments hit the string key while defaults use numeric. `Settings:normalizeSegment()` converts integer-looking segments; `normalizeRankKeys()` collapses old string duplicates on load.
- **Settings UI:** `InterfaceOptions_AddCategory` is NIL on 2.5.5 — use `Settings.RegisterCanvasLayoutCategory` + `RegisterAddOnCategory` (+ `RegisterCanvasLayoutSubcategory`).
- **Spell casting (2026-08-18/19):** `CastSpellByID`/`CastSpellByName` work OUT OF COMBAT for INSTANT spells **only in safe zones** (TrackingEye casts instant tracking spells with `pcall(CastSpellByID, id)`); outside safe zones even instant casts are blocked, and cast-time casts are blocked everywhere (see the cast-block bullet). `CastSpellByName(name, target)`'s target arg is pre-4.0 legacy — use `TargetUnit(unit)` + cast + `TargetLastTarget()` or the secure button's `unit` attribute. In combat everything insecure is PROTECTED → the engine's cast path is the secure hotkey.
- **Cast-time spells BLOCKED even OOC; instant spells blocked outside safe zones (2026-08-18/19, critical):** insecure casting (`CastSpellByName`/`CastSpellByID`) of cast-time spells is blocked on 20506 even OOC (safe zone, `ForceTaint_Strong` popup; SENT never fires). In the open world even INSTANT insecure casting is blocked (bare `CastSpellByName("Fel Armor")` → popup, no GCD, no buff). `SecureActionButtonTemplate:Click()` is silently dropped; `RunBinding()` (spell or macro) is also blocked. **Only a real key press can cast on 20506** → route ALL steps through a hidden secure button bound via `SetBindingClick` (M6/ItemRack pattern) and wait for the user's press.
- **Cast events (2026-08-18):** `UNIT_SPELLCAST_SENT/START/STOP/SUCCEEDED/INTERRUPTED/FAILED` all registered on 20506 (ItemRack, GatherMate2, M6). `UNIT_SPELLCAST_SENT` = modern `(unit, target, castGUID, spellID)` (GatherMate2 reads arg4 as spellID). `UNIT_SPELLCAST_SUCCEEDED` args are AMBIGUOUS — TrackingEye reads `select(2)` as spellID vs the retail `(unit, castGUID, spellID)`; consume the event as a signal only, verify via `UnitCastingInfo("player")` + GCD.
- **GCD/cooldown (2026-08-18):** `GetSpellCooldown(spellID)` returns 5 values `start, duration, enabled, modRate, active` (M6 Handlers.lua:195); GCD shows as `duration ~= 0` on any spell → wait for `duration == 0` between steps. `IsPlayerMoving()` exists (BetterFishing, M6). `SpellStopCasting()` global presence UNVERIFIED (no 20506 addon uses it) — use `/stopcasting` macro instead.
- **Keybindings (2026-08-18):** full API works on 20506 — `BINDING_HEADER_*`/`BINDING_NAME_*` (BetterFishing), `SetBinding`/`GetBindingAction`/`SaveBindings`/`GetCurrentBindingSet` (GatherMate2), `SetBindingClick` + hidden `SecureActionButtonTemplate` + `RegisterForClicks("AnyDown")` (ItemRack). `SetOverrideBindingSpell`/`SetOverrideBindingClick` also present (BetterFishing).

## Decisions

- **No Ace libraries.** Vanilla + `C_*` API only; timers via `ACP.Utils.Timers` (thin C_Timer wrapper mirroring Gargul's `GL:after/interval` API).
- **Orchestrator separate from execution.** `DeliveryController` (what to do) vs `TradeManager` (how) — decision logic unit-testable without the game.
- **One trade per partner, not per prep.** Runtime-only `givenTo` set (reset on `ACP_BUFF_LOST`, never persisted) — 2v2 = one trade per prep; 3v3/5v5 = next eligible partner.
- **Bracket gate on `ACP_BUFF_GAINED`** via `Settings:get("brackets." .. bracket)`; default 2v2 only.
- **Gate safety:** no new trades once `getRemainingTime() < gateSafetySeconds` (default 15); an open window is left untouched.
- **`givenTo` is runtime-only** — never persist to `ArenaChillPrepDB`.
- **`Settings:_init` deep-copies the defaults** (was `shallowCopy`): a shallow copy let `Settings:set` write THROUGH the shared nested tables and permanently corrupt `ACP.Data.DefaultSettings` — which also broke "Reset to defaults" (`reset()` deep-copies the (corrupted) defaults). `Settings:reset()` = `Data = Tables:deepCopy(DefaultSettings)` + persist + `OptionsUI:refresh()`. `Utils/Tables` gained `deepCopy`.
- **UI widgets are presentation-only** (`ACP.UI.*`): callers pass `getter`/`setter` closures; tooltips attached via `GameTooltip` `ANCHOR_RIGHT`, `wrap=true`. `Checkbox`/`Slider` expose `.label` so the caller can gray out labels on disable.

## To verify in game (pending)

- 3v3/5v5 end-to-end (code kept, UI locked for v0.1) — per-partner continuation when a second batch is crafted after the first trade.
- Master healthstone item 22105 rank pairing with 19012/19013 (Major) on a max-rank Warlock client (later verified implicitly via the 2026-08-22 rank-pair live tests — rank 5 conjures 19013).
- **UI redesign (2026-08-18):** General + Autotrade tabs render in the Settings list; tooltips on every control; "Reset to defaults" restores defaults; master switch off grays out Autotrade; divider between Brackets and Timing in the left column; `/console scriptErrors 1` clean. (A status line on General was tried and removed at the user's request.)
- **Workflow engine (2026-08-18):** Phase 7 data layer verified in game (catalog dump, `validateStep` → `true`, settings dot-path). **Phase 8 engine core implemented + out-of-game verified** (2026-08-18): `Classes/WorkflowEngine.lua` (IDLE→RUNNING→PAUSED→DONE, per-step gates incl. `knowsSpell = IsPlayerSpell(name)` + `IsPlayerSpell(spellID)` fallback, cast-time wait via `UNIT_SPELLCAST_*` signal + `UnitCastingInfo` verify + 10 s timeout, instant/GCD wait via `GetSpellCooldown` poll, createItem via `ACP_ITEMS_CHANGED` + 10 s poll net, target mgmt `TargetUnit`→cast→`TargetLastTarget`, named timers, buff-lost reset); wired in `bootstrap.lua` (after TradeManager, before DeliveryController), TOC, `OptionsUI` (`/acp workflow <N>`, `/acp status` workflow line), Localization help. Verified: 10-scenario smoke test green, full suite 170/170 @ 96.70%, vararg 18 files, syntax 46 files, deployed 20 files. **2026-08-19 pivot applied** (see status bullet): ALL steps route through the `ACPWorkflowButton` secure button bound to `/acp bind <key>`; waitingForKey→waitingForCast on SENT/START; instant steps via the effect-detection watch (SENT does not fire for instant on 20506); `UNIT_SPELLCAST_INTERRUPTED` re-arms the step (F9 resumes); `noHotkey` pause; `/acp bind`/`/acp bind off` commands; smoke test simulates key presses. **In-game DoD DONE (2026-08-19, user-confirmed):** 3-step F9 run (Fel Armor → Summon → Healthstone → DONE) + interrupt-resume.

## Phases

- Phase 0 — scaffold: DONE.
- Phases 1–5 — prep/bracket/countdown detection, bag scan, orchestrator, trade automation, UI/settings: **live-verified in arenas** (2v2 full cycle, `givenTo`, gate safety, bracket gate).
- Phase 6 — hardening (trade-open timeout, 3v3 continuation, stray-failure guard, death check): DONE (2026-08-09, CHANGELOG).
- Phase 7 — workflow engine data layer (spell catalog, step schema, constants, defaults, localization, TOC): DONE + in-game verified (2026-08-18).
- Phase 8 — workflow engine core (`WorkflowEngine.lua`, bootstrap/TOC/OptionsUI wiring, `/acp workflow`): implemented + out-of-game verified (2026-08-18); **hotkey pivot for ALL steps** (2026-08-19) — implemented + out-of-game verified; **FULLY verified in game (2026-08-19): 3-step F9 run + interrupt-resume confirmed by user**. In-arena prep run optional/pending.
- Phase 10 — workflow UI (`Widgets.lua` Dropdown/TextInput/ScrollFrame + `WorkflowUI.lua` + OptionsUI Workflows tab): **implemented + out-of-game verified (2026-08-20)** — 170/170 @ 96.99%, vararg 20 files, syntax OK, deployed 23 files. **P0 UI refactor (2026-08-20, per review):** vertical-cursor layout (no magic offsets), status line w/ engine state + CTA button (no gray-out when engine off — banner instead), compact toolbar (selector + enable + activation key of selected slot + name + Key Bindings hint), steps list as dominant area (~60% height, subtle gray-gold border), step rows with 30px `Up`/`Dn`/`X` buttons + Up/Down end-disable + red delete hover + dim "Self"/"—" labels. **Ghost-row fix (2026-08-20):** `renderSteps()` now hides retired rows and reparents them to a hidden recycle frame; never use `SetParent(nil)` on 2.5.5 because it promotes old rows/buttons to visible top-level frames (they survived refresh and appeared in the game world after closing Settings). **Empty-state fix (2026-08-20):** UI uses a separate `emptyWorkflowEditor` localization key from the runtime/log `emptyWorkflow` key, and explicitly hides the old FontString before rebuilding rows; key-binding hint uses ASCII `>` text because Unicode arrows render as boxes. Syntax/tests/vararg OK, deployed. In-game DoD pending user verification.
- Workflow model follow-up (2026-08-20): `ArenaChillPrepCharDB.workflows` now owns workflow settings per character; old account-wide `ArenaChillPrepDB.workflows` migrates on first load. `slotCount` starts at five and `+` creates empty slots up to 20 static binding entries. Movement pause is unconditional; `pauseOnMove` is removed from defaults/UI. `WorkflowSpellbook` scans active learned spells on 20506, groups ranks by localized name, and the UI stores an exact selected rank per step; the secure button now receives the exact spellID instead of resolving the highest rank by name. Direct keybind capture is available in the Workflows toolbar. Out-of-game checks: 170/170, syntax OK, vararg 21 files; deployed 24 files; in-game verification pending.
- **Workflow selector dropdown bugs (2026-08-20, user-reported):** (1) the workflow dropdown showed "Custom" on panel open instead of slot 1; (2) "+ Add workflow" did not update the dropdown; (3) "Delete workflow" should select the PREVIOUS workflow and render its steps. Root cause for (1)+(2): on 2.5.x `UIDropDownMenu_SetSelectedValue`/`UIDropDownMenu_Refresh` do NOT propagate the selection to the collapsed label (it stays the template default "Custom" until the menu opens; after add/delete the label kept the stale previous selection — internal `SelectedSlot` + steps were correct all along). Fix: `UI.Dropdown` now resolves the current value's label from its OWN item list and applies it EXPLICITLY via `UIDropDownMenu_SetText(frame, label)` on every build and refresh (`applyLabel()`), clearing `selectedValue` + placeholder text when nothing is selected — the same pattern every working 20506 addon uses (Gargul, Ranker, BGHistorian, AtlasLoot, ItemRack, WeakAuras). Fix for (3): `WorkflowUI:deleteWorkflow` now sets `SelectedSlot = math_max(slot - 1, 1)` (previous workflow) instead of `math_min(slot, count - 1)` (the shifted-up next one). Checks: 170/170 @ 97.22%, syntax/vararg OK, deployed 24 files. Gotcha recorded in `wow-api-20506/SKILL.md`.
- **Keyboard-swallow bug (2026-08-20):** user reported that after `/reload` nearly all keys died (Esc/Enter/WASD/hotkeys); disabling the addon restored them. Root cause: `UI.Keybind`'s capture button kept keyboard focus after capture ended while still holding an `OnKeyDown` handler, so it consumed every key. Fixed by installing the handler ONLY during capture and calling `ClearFocus()` + `SetScript("OnKeyDown", nil)` + `SetPropagateKeyboardInput(true)` in `stopCapture()`. Also hardened against `bindings-cache.wtf` corruption: captured keys are validated (uppercase letters/digits + optional `SHIFT-/CTRL-/ALT-` prefix) and `WorkflowUI:setSelectedKey` wraps `SetBinding`/`SaveBindings` in `pcall`; added `bindingInvalid` localization (enUS/ruRU). Tests 170/170 @ 97.29%, syntax/vararg OK, deployed 24 files. Gotcha recorded in `wow-api-20506/SKILL.md`.
- **Workflow editor usability redesign (2026-08-20):** Workflows tab rebuilt as three visual groups + a steps table (UI-only; no runtime/persistence changes). (1) Removed the duplicated key status — the bound key now appears ONLY in the editable Key field; the status line keeps engine + slot + step count. (2) `UI.Keybind` gained an 11th `captureText` param, three display states (unbound `Click and press a key` / capturing `Press a key...` + gold lock highlight / bound key), and `IsCapturing()`/`HasBinding()`; a separate `Clear` button (disabled while unbound) sits beside it. (3) The toolbar split into **Workflow defaults** (`skipIfBuffedDefault` + description) and **Workflow editor** (labeled `+ Add workflow`, Name + Enabled, Key + Clear). (4) Add Step now labels every entry with rank + spellID (`Create Healthstone - Ranks 1-5 (SpellIDs: ...) | default: Rank 5 (SpellID: ...)`); `spellID` is the disambiguator. (5) Step rows are 48px two-line cells (name+rank / `SpellID: <id>`), fixed Target and "Skip if buffed" columns render an explicit disabled `Not available` (+ tooltip) instead of `Self`/`—`, actions are right-aligned `↑`/`↓`/`Delete`. (6) The step list fills the remaining panel height (`updateStepScroll()` recomputes from content height + `stepListY`). (7) `UI.Box` background alpha 0.4→0.75 for contrast; status line neutral gray (no per-state accent colors). Checks: 170/170 @ 97.29%, syntax/vararg OK, deployed 24 files, `.ai/` docs updated. In-game QA (bright/dark backgrounds, capture states, Escape/click-cancel, scroll fill) pending user verification.
- **Add Step / rank simplification (2026-08-20, user-reported):** the "+ Add step" dropdown now lists PLAIN spell names only (no rank range, no SpellIDs, no default-rank decoration — `group.name` is the label). The per-step rank dropdown is REMOVED entirely — it never worked on the live client; every step now always stores the HIGHEST learned rank's exact spellID (same as the old add-default: `addStep` already picked `group.entries[#group.entries]`, entries sorted by rank ascending). Deleted: `WorkflowUI` `rankItems`/`setStepRank`/`formatSpellEntry` (folded into `formatSpellGroup` = plain name), `RANK_W`, `WorkflowSpellbook:getGroupForStep` (dead), and the workflow-editor rank localization keys (enUS + ruRU) — the Autotrade delivery `rankTooltip`/`ranks`/`rankLabel` strings are untouched. Step rows: name width now extends to the target column (`spellW - TARGET_X_SHIFT`), no rank control, no static rank text. Checks: 192/192 @ 95.86%, syntax 50 OK, vararg 21 OK, LuaHelper clean; docs updated (CONTEXT/ARCHITECTURE/plan). Deploy + in-game verification pending.
- **Class-gated spell catalog (2026-08-20, user-reported on a Mage):** the Add Step list showed Warlock spells on a non-Warlock character. Root cause: `WorkflowSpellbook:_init()` runs at ADDON_LOADED (before the character/spellbook is loaded), the initial scan returns nothing, and `addStaticFallback()` injected the hardcoded Warlock catalog unconditionally. Fix: (1) `addStaticFallback` is now class-gated — it only injects the catalog when `select(2, UnitClass("player")) == "WARLOCK"` (CLASS_WARLOCK guarded like Data/Items.lua); (2) the spellbook re-scans on PLAYER_LOGIN (character ready) in addition to SPELLS_CHANGED; (3) the UI's **Equip items** category is Warlock-only. Result: the Add Step list is always built from the active character's real spellbook — a Mage sees their own spells (grouped under Other spells), never Warlock spells. Tests 192/192 @ 95.86%, syntax/vararg OK, LuaHelper clean; docs updated. Deploy + in-game verification on a Mage pending.
- **Pet abilities + per-rank stone names (2026-08-20, user-reported):** the Warlock list is completed with the pet abilities Fire Shield (Imp) and Sacrifice (Voidwalker, spell 7812) as a new `pet` step type, and the stone-creating spells are listed per rank with the stone's name (`Create Master Healthstone`, `Create Major Healthstone`, …) so any rank can be picked. Pet steps are cast via the secure button with `type="macro"` + `/cast <name>` and are exempt from the player casting/movement gates; a pet step following a cast-time step is armed during that cast (PostClick of the secure button) so the pet ability can be pressed before the cast finishes.
- **Fire Shield / Unending Breath / Shadow Ward fix (2026-08-21, user-reported — applied from a reverted stash, code-only, no commit):** Fire Shield used the wrong TBC pet-ID `19483` (GetSpellInfo returned "Immolation" on 2.5.5 → row showed "Immolation (imp)"). Correct Imp Fire Shield is `27269` (`canTargetParty=true`); pet steps now get a Target dropdown (`buildStepRow` + `addStep` store `target="player"`, `petAbility` bakes `[target=partyN]` into the macro). Unending Breath (buff, `canTargetParty=true` in catalog) was missing a usable target choice because (a) saved steps with a non-catalog rank ID lost metadata in `findSpell` → fixed with a name-based fallback, and (b) `UIDropDownMenu_SetSelectedValue` overwrote the collapsed dropdown label on 2.5.x → fixed by re-applying `applyLabel()` after selection in `UI.Dropdown`. Shadow Ward was missing from the Buffs catalog → added max-rank `28610` (ranks 6229/11739/11740/28610 confirmed in GladiatorlosSA2/OmniBar/OmniCD/SweepyBoop), `canTargetParty=false` (self-only). The full-scan probe fix from the earlier stash was DELIBERATELY NOT applied — it exposed every learned spell in the "+ Add step" dropdown (no scroll), which the user rejected in favor of the limited buff/summon/pet-ability list. Tests 200/200 @ 95.24%, syntax/vararg OK, LuaHelper clean (only pre-existing annotate-type warnings). User declined commit and declined the extra petAbility-dedup / skip-if-buffed-for-pets / saved-step-migration items.
- **Detect Invisibility target choice (2026-08-21, user-reported):** the buff catalog entry for spell 132 had `canTargetParty=false`, so its step row rendered "Not available" for Target even though it is a party-castable buff (LibRangeCheck-3.0 lists 132 under `FriendSpells.WARLOCK`, 30 yd). Flipped the flag to `true`; the cast-step row now shows the Target dropdown (the row/dropdown condition uses `entry.canTargetParty`, already in place). Regression assert added in `testStaticFallbackIncludesPetsAndStoneRanks`.
- **Stone-step label fix (2026-08-21, user-reported — restored after the stashed bundle was dropped):** adding a `Create Master Healthstone` step showed only "Create Healthstone" (GetSpellInfo returns the plain name for every rank), so the rank was only distinguishable by SpellID. `WorkflowUI` got a `stepSpellLabel(step, entry)` helper that routes stone-rank steps through `WorkflowSpellbook:stoneStepLabel` (e.g. "Create Master Healthstone" / "Create Major Healthstone"); both non-pet name cells in `buildStepRow` now use it. The underlying `stoneStepLabel` was already unit-tested (test_workflows.lua). Tests 200/200 @ 95.24%, syntax/vararg OK.
- Tests — automated suite (169 tests, 96.45%): DONE (2026-08-11). In-game acceptance per change remains user-driven.

- **WorkflowEngine `resolveCastInfo` nil crash + rank-resolution regression (2026-08-21, user-reported via `/acp workflowtest`, error `WorkflowEngine.lua:420: attempt to call a nil value` on the `Summon Imp` step):** TWO bugs in the staged healthstone-rank changes. (1) `resolveCastInfo` had been pasted INSIDE `getCatalogEntry`'s body (between the `if (not spells) then return nil; end` and the catalog loop), so it was only assigned as a side effect of `getCatalogEntry` reaching that line — which never happened for spellbook-known spells (Summon Imp 688 is merged into the runtime spellbook), leaving `WorkflowEngine.resolveCastInfo` nil and crashing `castSpell`. Fixed by making `resolveCastInfo` a top-level method. (2) `pcall(IsPlayerSpell, spellID)` returns `(true, result)`; `if (IsPlayerSpell and pcall(...))` used only pcall''s success flag (the first value), so the "known" branch was ALWAYS taken and rank resolution NEVER upgraded an unlearned lower rank (11730 stayed 11730 instead of 27230). Same bug in `knowsSpell` and `Spellbook:getHighestKnownRank`. Fixed by capturing the RESULT: `local ok, known = pcall(IsPlayerSpell, spellID); if (ok and known) then`. (3) The new suite `Classes/test_workflowengine.lua` was never added to the `suites` list in `Tests/run_tests.lua`, so the corruption passed the suite (200/200) until registered. Added it + a reload-based regression test. Fix verified: 208/208 @ 95.15%, syntax 51 OK, vararg 21 OK. GOTCHA: a `function X:y()` written inside another function body is VALID Lua (it assigns `X.y` at runtime, not at load) and slips past luac/syntax-check — methods must be top-level; ALWAYS register new suites in `run_tests.lua`; NEVER rely on `if (pcall(fn, ...))` to read pcall''s second value.

- **Healthstone workflow stall on a lower rank (2026-08-21, user-reported via workflow 3 "test", step 2 = Major Healthstone 11730, stuck after step 1 = Master 27230):** root cause = `resolveCastInfo` cast the STORED rank when `IsPlayerSpell(stored)` was truthy, but on 20506 the client can report an UNLEARNED lower rank (11730 at level 70) as "known" while refusing to cast that spellID — the secure button pointed at 11730 did nothing → silent stall. Hardened `resolveCastInfo` to ALWAYS return the highest KNOWN rank of the spell family (via `getHighestKnownRank`, which filters by IsPlayerSpell): for a level-70 Warlock that is rank 6 (27230), so step 2 upgrades and casts normally; if the stored rank IS the top known rank the result is unchanged. Removed the fragile `IsPlayerSpell(stored)` short-circuit. Verified catalog IDs vs Wowhead/TBC DB: 6201/6202/5699/11729/11730/27230 = Create Healthstone ranks 1-6 (Minor→Master), items 19004/19006/19008/19010/19012/22105; 11730 = Major (rank 5) is correct (user suspected a wrong id — it is not). Added regression `testResolveCastInfoUpgradesEvenWhenLowerRankReportedKnown`. Tests 209/209 @ 95.15%, syntax 51 OK, deployed 24 files. GOTCHA: never trust `IsPlayerSpell(spellID)` for rank resolution — always resolve to the highest known rank of the family.

- **Healthstone rank resolution + label (correction, 2026-08-21):** user confirmed rank 5 (11730) IS learned and castable in-game as "Create Healthstone (rank 5)" — the previous "always cast the highest known rank" assumption was WRONG (it would force Master 22105 when the user wanted Major 19012). Restored `resolveCastInfo` (WorkflowEngine.lua) to: cast the stored rank verbatim when `IsPlayerSpell` is true; only upgrade to the highest KNOWN rank of the family when the stored rank is UNLEARNED. REAL underlying bug found and fixed: stone ranks were grouped in the spellbook by their rank-suffixed localized name ("Create Healthstone (rank 5)") while `getHighestKnownRank` was queried with the family name ("Create Healthstone"), so the upgrade lookup never matched. `addEntry` (WorkflowSpellbook.lua) now keys stone ranks by the family name (`stoneRanks[spellID].spellName`), matching `mergeStaticWarlock`. Also fixed `stoneStepLabel` to show the real in-game spell name ("Create Healthstone (rank 5)") instead of the synthesized "Create Major Healthstone". Tests 210/210 @ 95.30%, syntax OK, vararg OK, deployed. GOTCHA: never force-upgrade a castable stored rank; "Create Major Healthstone" is not a real spell — the spell is "Create Healthstone (rank N)" and creates a "Major Healthstone" item.

- **Pet-ability step stalls the workflow (2026-08-21):** a cast-time step (e.g. Summon Imp 688) immediately followed by a pet step (e.g. Fire Shield 27269) arms the pet ability to be cast DURING the player''s cast. But `onKeyPressed` (WorkflowEngine.lua:490) calls `releaseCastKey()` when the player cast is accepted, restoring the hotkey to the workflow RESUME action — and the arming block did NOT re-take the key over the secure button. So the pet macro''s 2nd press fired the resume action, `onSecurePress` never ran, and the engine sat in `waitingForPet` forever ("Fire Shield doesn''t get pressed / wf stalls"). FIX: in the arming block, after re-pointing the button to the pet macro, call `takeoverCastKey` again so the key clicks the button during the cast. Added regression `testOnKeyPressedArmsPetStepAndKeepsKeyTakenOver` (needs `GetBindingAction`/`GetBindingKey` stubs). Tests 211/211, syntax OK, vararg OK, deployed. GOTCHA: any code path that arms a follow-up step must leave the cast key taken over by the secure button, or the follow-up press silently never registers.

- **Pet-ability needs an explicit target in its macro (2026-08-21):** the real reason Fire Shield (27269) did not cast was targeting, not the key-takeover. A plain "/cast Fire Shield" only fires when the PLAYER has a target selected; the Imp then shields that target and otherwise does nothing. FIX: `petAbility` (WorkflowEngine.lua, both identical copies) now ALWAYS bakes an explicit target — self/missing target -> "/cast [target=player] <name>", party target -> "/cast [target=partyN] <name>". Added regression `testPetAbilityMacroBakesExplicitTarget`. Tests 212/212, syntax OK, vararg OK, deployed. GOTCHA: pet abilities are cast by the pet and need [target=unit] in the macro; never emit a bare "/cast <pet ability>" or it silently no-ops without a player target.

- **/acp workflowtest only runs once (2026-08-21):** `start()` no-ops when `state == "DONE"` (a new arena normally resets via ACP_BUFF_LOST, which never fires for the outside-arena test driver), so a 2nd `/acp workflowtest` did nothing until /reload. FIX: in `start()` (WorkflowEngine.lua), when `debugBypass` is true, reset a prior DONE/RUNNING/PAUSED run before the state guards so the workflow re-runs from step 1. Added regression `testStartInDebugBypassRerunsAfterDone`. Tests 213/213, syntax OK, vararg OK, deployed. GOTCHA: keep the DONE-guard reset gated behind `debugBypass` so real arena flow still requires a fresh arena to reset.

- **Architecture refactor Phases 0-7: DONE (2026-08-24)** — behavior-preserving consolidation + god-object splits per `.ai/docs/architecture-review-refactoring-plan.md` (details in the status bullets above). Two latent truncation bugs found and reported — both RESOLVED: the real spellbook scan never ran → scanner DELETED (user decision); a slot's secondary key never cast → BOTH keys now override (fixed + user-verified in game). W15 (possibly-dead-on-target defensive code) awaits the user's `/dump` pass (see the pending list). Phase 5's handler-table dispatch verified by the user in game (all 5 step types work).
- **Phase 11 — integration + docs: DONE (2026-08-24)** — code-level review (independent event-driven pipelines, no conflicts) + ARCHITECTURE.md §2.10 WorkflowEngine + edge-case rows. In-arena DoD checklist handed to the user (pending).
- **Phase 12 — unit tests: DONE** (277/277, coverage 91.93%, suites incl. test_workflowengine/test_workflowrepository/test_workflowkeybindcontroller).

- **Workflow Name/Enabled bound to slot 1 (2026-08-24, user-reported):** changing the Workflow selector never updated the Name TextBox, and edits were always applied to workflow 1. Root cause: `WorkflowUI:buildEditor` bound the Name/Enabled getter/setter closures with `bindPath(definitionPath(self.SelectedSlot) .. ...)` at BUILD time — `SelectedSlot` is 1 then and the closures captured that slot forever (the selector setter + refresh re-render everything else, but these closures were never re-created). Fix: the four closures now resolve `definitionPath(self.SelectedSlot)` at CALL time (the same dynamic pattern the selector/keybind getters already used). The keybind getter was already dynamic (`selectedKey(self.SelectedSlot)`) and the static `workflows.skipIfBuffedDefault` bindPath stays. Checks: 274/274 tests @ 92.02%, syntax 65 OK, vararg 33 OK. GOTCHA: never capture a per-slot settings path in a build-time closure when the slot can change after build — resolve the path inside the getter/setter.
- **Bugfix: inbound trade (partner opens trade to us) now delivers prep items (2026-08-24, user-reported):** root cause - TradeManager.TRADE_SHOW only auto-placed items when self.trading was true (we initiated); a partner-initiated window left 	rading=false, so no stones were placed AND the DeliveryController stayed ACTIVE and kept polling/scheduling a redundant startTrade. Fix: (1) DC gained shouldTakeOverInboundTrade() (gates on ACTIVE + enabled + inArena + notInCombat + partner is a teammate) and onTradeOpened(partner) (cancels pending TradeDelay, credits currentPartner, moves ACTIVE->TRADING, queues items); (2) TM.TRADE_SHOW now takes over an inbound window when DC allows (sets trading=true, fires ACP_TRADE_OPENED); (3) DC.onItemsChanged now also refills the queue while TRADING and the window is open (stones crafted mid-trade get delivered, incl. inbound). Checks: 286/286 tests, coverage 90.05%.
